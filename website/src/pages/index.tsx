import React from "react";
import styled from "styled-components";

const Heading = styled.h1`
  font-family: sans-serif;
  font-size: 5rem;
`;

export default function Home() {
  return <Heading>Yakshaving Bundler</Heading>;
}
